package com.shivaa.recusions;

public class RemoveChar {
    public static void main(String[] args) {
        String str = "abcax";
        int size = str.length();
        char target = 'a';
        int index = 0;
        remove(str,index,size,target);
    }
    // function
    static String remove(String string,int idx,int n , char tar){
        if(string.charAt(idx)==tar){

        }
    }
}
